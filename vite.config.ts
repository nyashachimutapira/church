import os from "node:os";
import path from "node:path";
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import { VitePWA } from "vite-plugin-pwa";

// https://vite.dev/config/
export default defineConfig({
  // Avoid Windows/OneDrive EPERM locks by caching outside workspace.
  cacheDir: path.join(process.env.LOCALAPPDATA ?? os.tmpdir(), "church-vite-cache"),
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['favicon.svg', 'pwa-192x192.png', 'pwa-512x512.png'],
      manifest: {
        name: 'ZAOGA FIF Bestlea Region',
        short_name: 'ZAOGA Bestlea',
        description: 'ZAOGA FIF Bestlea Region Church Platform',
        theme_color: '#0b1120',
        background_color: '#0b1120',
        display: "standalone",
        icons: [
          {
            src: 'pwa-192x192.png',
            sizes: '192x192',
            type: 'image/png'
          },
          {
            src: 'pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png'
          },
          {
            src: 'pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png',
            purpose: 'any maskable'
          }
        ]
      }
    })
  ],
})
